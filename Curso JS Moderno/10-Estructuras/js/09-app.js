//Operadores ternarios para

const autenticando =true;

const puedePagar =true;

console.log( autenticando ? puedePagar ? "si esta autenticado y puede pagar" : "Si esta autenticado pero no puede pagar": "No puede pagar"); 

